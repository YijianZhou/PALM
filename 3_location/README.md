# Location

`hypodd/` contains the legacy differential-time construction and hypoDD
relocation workflow for MFT detections. Edit its `config.py` for the installed
hypoDD binary, station file, template phase file, detection phase file, and
case-specific thresholds before running it.
